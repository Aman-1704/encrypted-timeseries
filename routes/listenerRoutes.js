const express = require('express');

const router = express.Router();
const listenerController = require('../controllers/listenerController');

// router.ws('/data', listenerController.handleEncryptedData);

module.exports = router;